name: Release

on:
  push:
    tags:
      - 'v*'

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: '3.13'
      - name: Install dependencies
        run: pip install pytest pytest-cov
      - name: Run tests
        run: |
          python -m pytest tests/test_convert.py -q \
            --deselect tests/test_convert.py::TestIntegration::test_integration_conversion_with_mocked_online \
            --deselect tests/test_convert.py::TestIntegration::test_integration_conversion_m4a_with_mocked_online

  build-linux:
    needs: [test]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - uses: actions/setup-python@v6
        with:
          python-version: '3.13'

      - name: Install dependencies
        run: pip install pyinstaller

      - name: Download ffmpeg (static)
        uses: ./.github/actions/fetch-ffmpeg
        with:
          url: https://johnvansickle.com/ffmpeg/releases/ffmpeg-release-amd64-static.tar.xz
          output: ffmpeg.tar.xz
          checksum-url: https://johnvansickle.com/ffmpeg/releases/ffmpeg-release-amd64-static.tar.xz.md5
          min-bytes: 20000000

      - name: Extract ffmpeg
        run: |
          set -euo pipefail
          tar xf ffmpeg.tar.xz
          cp ffmpeg-*-static/ffmpeg pyinstaller/ffmpeg
          chmod +x pyinstaller/ffmpeg

      - name: Build binary
        run: pyinstaller pyinstaller/convert.spec

      - name: Create archive
        run: |
          cd dist/audioconvert
          tar czf ../audioconvert-linux-amd64.tar.gz *

      - name: Upload artifact
        uses: actions/upload-artifact@v5
        with:
          name: linux-amd64
          path: dist/audioconvert-linux-amd64.tar.gz

  release:
    needs: [build-linux]
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/download-artifact@v5

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          files: |
            linux-amd64/audioconvert-linux-amd64.tar.gz
          generate_release_notes: true
          draft: false
          prerelease: false
